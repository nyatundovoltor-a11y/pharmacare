# Blocks direct browser access to config/, core/, models/, controllers/,
# database/ etc. Only public/ should ever be reachable directly.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
